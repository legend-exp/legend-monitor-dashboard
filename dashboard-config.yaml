paths:
  base: ..
  cal: ..
  phy: /path/to/phy
  sipm: /path/to/sipm
  muon: /path/to/muon
  tmp: /tmp
  llama: /path/to/llama
  # editable legend-metadata clone for the Metadata editor page (cloned /
  # updated on startup; see METADATA_EDIT_URL)
  metadata_edit: ../metadata-edit
